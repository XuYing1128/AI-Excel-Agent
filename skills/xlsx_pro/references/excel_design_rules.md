# Excel 设计规则

- 首页优先放说明、Dashboard 或汇总，不让用户打开后面对原始数据。
- 数据源、计算区、汇总区、仪表盘分层，不把所有内容挤在一个 sheet。
- 表头固定在可冻结位置，数据区域启用自动筛选。
- 不在筛选数据区内部使用合并单元格。
- 列宽按内容设置上限，避免过宽或过窄。
- 金额、数量、日期、百分比格式应清晰一致。
- 高风险或关键假设放在明显输入区。

