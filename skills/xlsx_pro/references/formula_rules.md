# 公式规则

- 所有派生值写 Excel 公式，例如 `SUM`、`AVERAGE`、`COUNTIFS`、`SUMIFS`、`XLOOKUP`、增长率、毛利率、占比、ROI。
- 空输入行使用 `IF(关键单元格="","",公式)` 保护。
- 除法使用 `IFERROR(分子/分母,0)`。
- 查找公式优先使用 `XLOOKUP`，需要兼容旧 Excel 时用 `INDEX/MATCH`。
- 财务、利润、报价类公式必须引用输入单元格，不把税率、折扣、单价结果隐藏成常量。
- 公式填充范围必须和数据区一致。

